server:
  port: 8080


server:
  cloud:
    gateway:
      routes:
        id: route-products
        uri: http://localhost:6969
        predicates:
          Path: /api/v1/products/**
        id: route-user
        uri: lb://ms-user
        predicates:
          Path: /api/v1/user/**
